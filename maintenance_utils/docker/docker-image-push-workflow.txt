Optional:
    Tag the current commit: git tag -a "0.0.0" -m'initial version'

- Generate Docker-compose-yml-file:
    Specify a Git-Tag:
    sh ./prod_generate_compose_file.yml 0.0.0
    Otherwise the Docker-Images will be tagged as 'latest':
    sh ./prod_generate_compose_file.yml

- Login into DockerHub
  docker login --username=openbudgets

- Build Docker-Images:
    sh ./upload_docker_images
